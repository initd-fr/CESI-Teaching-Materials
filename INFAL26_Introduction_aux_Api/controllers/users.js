// Contrôleur utilisateurs — Logique métier des endpoints /users
// Les routes délèguent ici : pas de logique CRUD dans routes/users.js
// ~ IMPORTS
// TODO : importer les fonctions d'accès au store (data/store.js)

// & FONCTIONS (handlers)
// Chaque fonction reçoit (req, res). À vous d'utiliser le store et d'envoyer les bonnes réponses (status + json).

// TODO : getAll(req, res)     → GET /users      — lister tous les utilisateurs
// TODO : getById(req, res)   → GET /users/:id   — récupérer un utilisateur (req.params.id)
// TODO : create(req, res)   → POST /users      — créer (req.body : nom, email, age)
// TODO : updatePartial(req, res) → PATCH /users/:id — mise à jour partielle (req.body)
// TODO : updateFull(req, res)   → PUT /users/:id    — remplacement complet (req.body : nom, email, age)
// TODO : remove(req, res)   → DELETE /users/:id — supprimer

// TODO : exporter ces fonctions (module.exports ou exports.getAll = ..., etc.)
