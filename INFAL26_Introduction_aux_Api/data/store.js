// Stockage en mémoire des utilisateurs — À compléter par l'étudiant
// Pas de base de données : un simple tableau en mémoire suffit.

// Structure d'un utilisateur : { id, nom, email, age }

// TODO : initialiser un tableau (ex. `let users = [];`) pour stocker les utilisateurs.

// TODO : implémenter des fonctions d'accès, par exemple :
// - getUsers()           → retourne tous les utilisateurs
// - getUserById(id)      → retourne un utilisateur ou undefined
// - addUser(nom, email, age) → génère un id, ajoute l'utilisateur, le retourne
// - updateUser(id, data) → met à jour (partiel ou total selon l'appelant)
// - deleteUser(id)      → supprime l'utilisateur, retourne true/false

// TODO : pour addUser, générer un id unique (ex. max des id existants + 1, ou Date.now()).

// TODO : exporter ces fonctions pour les utiliser dans routes/users.js
